import Vue from 'vue'
const { faqs } = require('~/external/data/faqs.js')

const state = () => ({
  faqs
})

const getters = {
}

const mutations = {
}

const actions = {
}

export default {
  state,
  getters,
  mutations,
  actions
}
